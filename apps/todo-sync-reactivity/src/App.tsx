import Ref from "./Ref";
import Sync from "./Sync";

export default function App() {
  return (
    <main className="page">
      <div className="demo-grid">
        <Ref />
        <Sync />
      </div>
    </main>
  );
}
